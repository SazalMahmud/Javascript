let preyoPerson = 'Tumpa';
const hubby = 'sazal';

function getName(first , last='monir'){
    return first + ' ' +last;
};

 const  Myfamily = ` amr family ${hubby} and good member ${getName('shamim --')}. happy family ${preyoPerson}`
 console.log(Myfamily);
